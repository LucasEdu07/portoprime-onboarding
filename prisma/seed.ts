// Seed opcional. Os parâmetros de plano por modalidade são estáticos e vivem em
// src/lib/consts.ts (não precisam de banco). Mantemos este arquivo como ponto de
// extensão para a fase 2 (ex.: popular tabelas de catálogo do CRM).
async function main() {
  console.log("Seed: nada a popular no MVP (parâmetros vivem em src/lib/consts.ts).");
}

main();
